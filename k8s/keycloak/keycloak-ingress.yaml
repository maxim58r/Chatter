apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak
  namespace: keycloak
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: msv-tech.ru
      http:
        paths:
          # Разрешаем доступ к .well-known без перенаправления
          - path: /.well-known/acme-challenge
            pathType: Prefix
            backend:
              service:
                name: keycloak
                port:
                  number: 8080
          # Все остальные запросы перенаправляем на HTTPS
          - path: /
            pathType: Prefix
            backend:
              service:
                name: keycloak
                port:
                  number: 8080
  tls:
    - hosts:
        - msv-tech.ru
      secretName: msv-tech.ru-tls

# kubectl apply -f keycloak-ingress.yaml -n keycloak