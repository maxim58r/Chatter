apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak
  namespace: keycloak
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  rules:
    - host: msv-tech.ru
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: keycloak
                port:
                  number: 80
          - path: "/.well-known/acme-challenge/"
            pathType: Prefix
            backend:
              service:
                name: cm-acme-http-solver-pfcs6  # Подставь правильное имя challenge-сервиса
                port:
                  number: 80
  tls:
    - hosts:
        - msv-tech.ru
      secretName: keycloak-tls
