apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | default "messagingservice" }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.name | default "messagingservice" }}
  ports:
    - protocol: TCP
      port: {{ .Values.containerPort }}
      targetPort: {{ .Values.containerPort }}
