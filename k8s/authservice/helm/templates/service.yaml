apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | default "authservice" }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.name | default "authservice" }}
  ports:
    - protocol: TCP
      port: {{ .Values.containerPort }}
      targetPort: {{ .Values.containerPort }}
