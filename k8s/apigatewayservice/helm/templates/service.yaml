apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | default "apigatewayservice" }}
spec:
  type: LoadBalancer
  selector:
    app: {{ .Values.name | default "apigatewayservice" }}
  ports:
    - protocol: TCP
      port: {{ .Values.containerPort }}
      targetPort: {{ .Values.containerPort }}