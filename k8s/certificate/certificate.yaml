apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: msv-tech-tls
  namespace: keycloak
spec:
  secretName: msv-tech-tls
  dnsNames:
    - msv-tech.ru
    - "*.msv-tech.ru"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer