apiVersion: v1
kind: Service
metadata:
  name: redis-shared-nodeport
  namespace: redis
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: redis-shared
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 32179
