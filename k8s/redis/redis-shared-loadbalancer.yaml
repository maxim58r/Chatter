apiVersion: v1
kind: Service
metadata:
  name: redis-shared-lb
  namespace: redis
spec:
  type: LoadBalancer
  selector:
    app: redis-shared   # ← соответствует Pod
  ports:
    - port: 6379
      targetPort: 6379
