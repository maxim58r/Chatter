apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: local-lan-pool
  namespace: metallb-system
spec:
  addresses:
    - 192.168.1.240-192.168.1.250
  autoAssign: true
  avoidBuggyIPs: false
  avoidAddressSharing: false
  serviceAllocation:
    allowSharedIP: false
  # Ограничиваем пул только до IP, назначенного на узле
  allowedAddresses:
    - 192.168.1.35/32
