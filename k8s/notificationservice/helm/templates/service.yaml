apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | default "notificationservice" }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.name | default "notificationservice" }}
  ports:
    - protocol: TCP
      port: {{ .Values.containerPort }}
      targetPort: {{ .Values.containerPort }}
